import React from "react";
import { Alert } from "react-bootstrap";

const Userdata = () => {
  return (
    <div>
      <Alert variant="success">
        <Alert.Heading>User Data</Alert.Heading>
        <p>Add User Data</p>
      </Alert>
      Userdata
    </div>
  );
};

export default Userdata;
