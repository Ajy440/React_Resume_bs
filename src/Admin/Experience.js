import React from "react";
import { Alert } from "react-bootstrap";

const Experience = () => {
  return (
    <div>
      <Alert variant="success">
        <Alert.Heading>Experience</Alert.Heading>
        <p>Add Experience Data</p>
      </Alert>
      Experience
    </div>
  );
};

export default Experience;
